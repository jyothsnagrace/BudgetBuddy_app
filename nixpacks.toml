[phases.setup]
nixPkgs = ["python312Full", "python312Packages.pip", "tesseract"]

[phases.install]
cmds = [
  "cd backend && pip3 install --upgrade pip",
  "cd backend && pip3 install -r requirements.txt"
]

[start]
cmd = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"

